name: Sila SIPs 100 Percent Regeneration

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Generator parked while corrected regeneration authority is installed."
